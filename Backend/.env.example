# Banco de dados
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=troque_a_senha
DB_NAME=caige
DB_PORT=3306

# Aplicação
PORT=3000
NODE_ENV=development

# Autenticação JWT
# Use uma chave longa, aleatória e exclusiva em produção.
JWT_SECRET=troque_por_uma_chave_segura_e_longa
JWT_EXPIRES_IN=8h
